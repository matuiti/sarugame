import { Sounds, Obj } from './index.js';
import { GAME_STATE, SARU } from './index.js';

class Unti extends Obj {
  constructor() {
    super("unti");
  }
  draw(ctx) {
    super.draw(ctx);
  }
}

export default Unti;